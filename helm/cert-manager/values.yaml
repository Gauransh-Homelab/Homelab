# Cert-Manager Helm Chart Values
# Automatic certificate management for Kubernetes

# Enable CRD installation  
crds:
  enabled: true
  keep: true

# Global settings
global:
  # Disable PodSecurityPolicy (deprecated in Kubernetes 1.21+)
  podSecurityPolicy:
    enabled: false
  
  # Leader election namespace - using cert-manager namespace
  leaderElection:
    namespace: cert-manager

# Override the namespace for storing DNS provider credentials for ClusterIssuer resources
clusterResourceNamespace: cert-manager

# Force cert-manager to use public DNS for ACME challenge verification
# Without this, cluster DNS hits random authoritative nameservers that may timeout
extraArgs:
  - --dns01-recursive-nameservers-only
  - --dns01-recursive-nameservers=8.8.8.8:53,1.1.1.1:53

# Prometheus monitoring (disabled for now)
prometheus:
  enabled: false

# Pod Security Context
securityContext:
  runAsNonRoot: true
  seccompProfile:
    type: RuntimeDefault

# Resource limits for homelab environment
resources:
  requests:
    cpu: 10m
    memory: 32Mi
  limits:
    cpu: 100m
    memory: 128Mi

# Node selector to run on Linux nodes
nodeSelector:
  kubernetes.io/os: linux

# Webhook configuration
webhook:
  resources:
    requests:
      cpu: 10m
      memory: 32Mi
    limits:
      cpu: 100m
      memory: 128Mi
  
  nodeSelector:
    kubernetes.io/os: linux

# CA Injector configuration
cainjector:
  enabled: true
  resources:
    requests:
      cpu: 10m
      memory: 32Mi
    limits:
      cpu: 100m
      memory: 128Mi
  
  nodeSelector:
    kubernetes.io/os: linux

# Startup API Check
startupapicheck:
  enabled: true
  resources:
    requests:
      cpu: 10m
      memory: 32Mi
    limits:
      cpu: 100m
      memory: 128Mi
  
  nodeSelector:
    kubernetes.io/os: linux