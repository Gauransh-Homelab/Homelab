apiVersion: v1
kind: Service
metadata:
  name: jellystat
  namespace: jelly
  labels:
    app: jellystat
spec:
  type: ClusterIP
  selector:
    app: jellystat
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
